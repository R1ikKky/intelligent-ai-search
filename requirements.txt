# Общие зависимости репозитория: beckend (Django), docker/dataset-loader (скрипт заливки).
# Виртуальное окружение создавайте в корне: python -m venv venv
#
# --- PostgreSQL (dataset-loader + Django) ---
psycopg[binary]>=3.2,<4
#
# --- Django API (beckend) ---
Django>=5.0,<6
djangorestframework>=3.15,<4
djangorestframework-simplejwt>=5.3,<6
drf-spectacular>=0.27,<0.29
django-cors-headers>=4.3,<5
gunicorn>=22,<24
python-dotenv>=1.0,<2
#
# --- Поиск СТЕ (Elasticsearch + эмбеддинги) ---
# Для NVIDIA GPU на ПК: после установки зависимостей поставьте torch с CUDA
# (см. https://pytorch.org/get-started/locally/ ); ste_search/embedding.py сам выберет cuda.
django-elasticsearch-dsl>=8.0,<9
elasticsearch>=8.12,<9
sentence-transformers>=3.0,<4
numpy>=1.26,<3
