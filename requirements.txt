# Общие зависимости репозитория: beckend (Django), docker/dataset-loader (скрипт заливки).
# Виртуальное окружение создавайте в корне: python -m venv venv
#
# --- PostgreSQL (dataset-loader + Django) ---
psycopg[binary]>=3.2,<4
#
# --- Django API (beckend) ---
Django>=5.0,<6
djangorestframework>=3.15,<4
djangorestframework-simplejwt>=5.3,<6
drf-spectacular>=0.27,<0.29
django-cors-headers>=4.3,<5
gunicorn>=22,<24
python-dotenv>=1.0,<2
